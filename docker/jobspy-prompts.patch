diff --git a/src/prompts/job-recommendations.js b/src/prompts/job-recommendations.js
index d713df3..8b39bcd 100644
--- a/src/prompts/job-recommendations.js
+++ b/src/prompts/job-recommendations.js
@@ -3,7 +3,7 @@ import { z } from 'zod';
 /**
  * Job Recommendations Schema - Parameters for job recommendations
  */
-export const jobRecommendationsSchema = z.object({
+export const jobRecommendationsSchema = {
   skills: z.string()
     .describe('A list of skills the job seeker possesses'),
   experienceLevel: z.string()
@@ -14,7 +14,7 @@ export const jobRecommendationsSchema = z.object({
     .describe('Areas of interest for the job seeker that can help guide job recommendations'),
   jobType: z.string()
     .describe('The type of job the seeker is looking for (e.g., full-time, part-time, contract, internship)'),
-});
+};
 
 
 /**
diff --git a/src/prompts/resume-feedback.js b/src/prompts/resume-feedback.js
index d64c5cc..694d37c 100644
--- a/src/prompts/resume-feedback.js
+++ b/src/prompts/resume-feedback.js
@@ -6,7 +6,7 @@ import { z } from 'zod';
 export const resumeFeedbackPrompt = server => server.prompt(
   'resume_feedback',
   'Get professional feedback on your resume for specific roles and industries',
-  z.object({
+  {
     resumeText: z.string()
       .describe('The full text of the resume to analyze'),
     targetRole: z.string()
@@ -15,7 +15,7 @@ export const resumeFeedbackPrompt = server => server.prompt(
       .describe('The industry the job seeker is targeting'),
     experienceLevel: z.string()
       .describe('The experience level of the job seeker (e.g., entry-level, mid-level, senior)'),
-  }),
+  },
   (inputs) => {
     return {
       messages: [
diff --git a/src/prompts/search-jobs.js b/src/prompts/search-jobs.js
index 78cccb6..26a290c 100644
--- a/src/prompts/search-jobs.js
+++ b/src/prompts/search-jobs.js
@@ -12,9 +12,9 @@ import { z } from 'zod';
 export const searchJobsPrompt = server => server.prompt(
   'search_jobs',
   'Extract job search parameters from a natural language query',
-  z.object({
+  {
     query: z.string().describe('Job search query'),
-  }),
+  },
   (inputs) => {
     return {
       messages: [
