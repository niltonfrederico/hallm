default_install_hook_types: [pre-commit, commit-msg]

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        exclude: ^k8s/
      - id: check-yaml
        name: check-yaml (k8s multi-doc)
        files: ^k8s/
        args: [ --allow-multiple-documents ]
      - id: check-toml
      - id: check-added-large-files
        name: check-added-large-files (non-image)
        exclude_types: [image]
        exclude: (^|/)(package-lock\.json|uv\.lock|poetry\.lock|yarn\.lock|pnpm-lock\.yaml|Cargo\.lock)$
      - id: check-added-large-files
        name: check-added-large-files (image, 1MB)
        types: [image]
        args: [ --maxkb=1024 ]
      - id: check-merge-conflict
      - id: debug-statements

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format

  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.22.1
    hooks:
      - id: markdownlint-cli2
        name: markdownlint (ai config)
        files: ^\.github/.*\.md$

      - id: markdownlint-cli2
        name: markdownlint (project docs)
        files: \.md$
        exclude: ^(\.claude|\.github)/

  - repo: local
    hooks:
      - id: ty
        name: ty (type check)
        entry: uv run ty check
        language: system
        types: [ python ]
        pass_filenames: false

      - id: commitlint
        name: commitlint
        entry: commitlint --edit
        language: system
        stages: [ commit-msg ]
